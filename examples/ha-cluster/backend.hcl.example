bucket = "powervs-${env}"
key    = "${env}/ha-cluster.tfstate"
region = "us-south"
endpoint = "s3.direct.us-south.cloud-object-storage.appdomain.cloud"
skip_region_validation      = true
skip_credentials_validation = true
access_key = "CHANGEME"
secret_key = "CHANGEME"
